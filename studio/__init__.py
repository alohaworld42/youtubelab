"""studio — multi-channel management, idea queue, scheduler and asset sourcing.

Sits on top of the existing `pipeline` package: pipeline renders one video,
studio decides *what* to render, *for which channel*, and *when*.
"""
